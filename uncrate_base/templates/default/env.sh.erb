#!/bin/bash

export UNCRATE_HOME=#{node['uncrate']['home']}

# Add uncrate binaries to the path
PATH=$UNCRATE_HOME/bin:$PATH

for f in $UNCRATE_HOME/env.d/*.sh ; do
	if [ -f $f ] ; then
		source $f
	fi
done
